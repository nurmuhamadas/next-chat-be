export enum TimeFormat {
  HALF_DAY = "12-HOUR",
  FULL_DAY = "24-HOUR",
}

export enum Language {
  ENGLISH = "en_US",
  INDONESIAN = "id_ID",
}

export enum Notifications {
  PRIVATE = "PRIVATE",
  GROUP = "GROUP",
  CHANNEL = "CHANNEL",
}
